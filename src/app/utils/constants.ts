import { LocalStorageKeysT } from '../models/local-storage.model';

export const LocalStorageKeys: Record<LocalStorageKeysT, string> = {
  notes: 'NOTES',
  tags: 'TAGS',
  users: 'USERS',
  sharedNotes: 'SHARED_NOTES',
};
